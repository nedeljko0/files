{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "reset": "rm -rf node_modules packages/*/node_modules && yarn install"
  },
  "dependencies": {
    "web-vitals": "^4.2.4"
  },
  "devDependencies": {
    "lerna": "^8.2.0"
  }
}
